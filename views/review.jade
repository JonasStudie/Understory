extends layout

block content
  h1= title
  p Dette er oversigten over anmeldelser.

  if reviews && reviews.length
    .cards
      each review in reviews
        if review.review_count > 0
          a.card(href='/review/' + review.id)
            h2= review.evnt_name
            p.company= review.company_name
            p.snippet= review.short_review
            p.rating= '⭐ ' + review.avg_rating+'/5' + ' (' + review.review_count + ' anmeldelser)'
  else
    p Der er ingen anmeldelser endnu.

